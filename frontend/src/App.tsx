/**
 * Placeholder root component.
 *
 * The UI has not been designed or built yet -- this exists only so the
 * scaffolding is complete and runnable. Replace it when frontend work begins
 * (see docs/milestones.md, M2).
 */
export default function App() {
  return <main>NexaAssist</main>;
}
