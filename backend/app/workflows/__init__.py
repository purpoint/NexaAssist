"""Specialised workflows.

``definition`` is the format a workflow is written in; ``execution`` runs one.
A workflow is a fixed, declared sequence of tool calls -- the deterministic
counterpart to the M7 agent, for the cases where the right steps are already
known and letting a model choose them adds risk rather than value.
"""
