"""Version 1 of the HTTP API.

Everything under this package is served at ``{API_PREFIX}/v1``. Breaking
changes to a published route mean introducing a ``v2`` package rather than
editing these modules in place.
"""
