"""Prompt text, versioned in the repository.

Empty for now -- no capability issues a prompt yet. The convention this module
exists to establish: every prompt is a named constant here, paired with a
version string, and mirrored into ``docs/prompt.md``. Prompts are product
surface, so they are reviewed as code rather than buried in an f-string at a
call site.
"""
